# Cursor Skin 使用操作手册

自定义 Cursor：**背景图 + 主题色 + 文件图标**。

---

## 一、第一次使用（已帮你装好时）

1. 按 `Ctrl + Shift + P` 打开命令面板  
2. 输入并执行：`Developer: Reload Window`（重载窗口）  
3. 重载后应看到：
   - 配色为 **Skin Dark**
   - 资源管理器图标为 **Air File Icons**
   - 工作台背后有半透明背景图

若没有生效，按下面「手动启用」操作。

---

## 二、手动启用（主题 / 图标）

### 1. 切换配色主题

1. `Ctrl + Shift + P`  
2. 输入：`Preferences: Color Theme`（颜色主题）  
3. 选择：**Skin Dark**

### 2. 切换文件图标

1. `Ctrl + Shift + P`  
2. 输入：`Preferences: File Icon Theme`（文件图标主题）  
3. 选择下列之一：
   - **Air File Icons**（默认推荐）
   - **Air Material Icons**
   - **Air + Material Icons**

---

## 三、命令面板里的皮肤命令

`Ctrl + Shift + P`，输入 `Cursor Skin`，会看到：

| 命令 | 作用 |
| --- | --- |
| **应用皮肤（背景图 + 主题色）** | 按当前设置重新生成配色，并写入背景图补丁 |
| **选择背景图** | 打开文件选择器，选本地图片后自动应用 |
| **设置主题色** | 输入十六进制颜色（如 `#7C9EF6`），自动重算配色 |
| **移除皮肤并还原安装目录** | 关掉皮肤、撤掉补丁，恢复原版安装文件 |
| **查看当前状态** | 显示目标安装路径、是否已打补丁、背景图路径等 |

改完配置后若提示重载，选 **立即重载**。

---

## 四、换背景图

### 方式 A：命令（推荐）

1. `Ctrl + Shift + P` → `Cursor Skin: 选择背景图`  
2. 选一张图片（支持 png / jpg / webp / gif / bmp / avif / svg）  
3. 按提示重载窗口  

### 方式 B：设置项

1. `Ctrl + ,` 打开设置  
2. 搜索：`cursorSkin.backgroundImage`  
3. 填本地绝对路径，例如：

```text
C:\Users\do996\Documents\cursor-skin\media\wallpaper-default.png
```

也可填 `https://` 开头的网络图片（不能用 `http://`）。

---

## 五、改主题色

### 方式 A：命令

1. `Ctrl + Shift + P` → `Cursor Skin: 设置主题色`  
2. 输入如 `#FF6B6B`、`#7C9EF6`  
3. 按提示重载  

### 方式 B：设置项

搜索 `cursorSkin.accentColor`，改成你想要的十六进制颜色。

主题色会影响：光标、按钮、选中高亮、徽标、焦点边框、标签页顶边等。

---

## 六、常用调节（设置里搜 `cursorSkin`）

| 设置项 | 默认 | 说明 |
| --- | --- | --- |
| `cursorSkin.opacity` | `0.16` | 背景图可见度，建议 `0.08`～`0.25` |
| `cursorSkin.blur` | `0` | 背景模糊（像素）。照片当背景可试 `16`～`32` |
| `cursorSkin.editorOpacity` | `0.7` | 编辑器不透明度，越低图越明显，但字可能更难读 |
| `cursorSkin.panelOpacity` | `0.82` | 侧边栏 / 标题栏 / 状态栏不透明度 |
| `cursorSkin.backgroundColor` | `#0F1115` | 工作台底色 |
| `cursorSkin.size` | `cover` | 图片缩放：`cover` / `contain` / `auto` / `100% 100%` |
| `cursorSkin.position` | `center center` | 图片位置 |
| `cursorSkin.customCss` | 空 | 追加自定义 CSS |
| `cursorSkin.enabled` | `true` | 设为 `false` 会在下次同步时还原安装目录 |
| `cursorSkin.installPath` | 空 | 指定要打补丁的安装目录；空 = 当前正在运行的这份 |
| `cursorSkin.fixChecksums` | `true` | 同步校验和，避免「安装似乎已损坏」提示 |

改这些设置后扩展会自动重新应用，一般只需再重载一次窗口。

---

## 七、卸载 / 还原成原版

1. `Ctrl + Shift + P` → `Cursor Skin: 移除皮肤并还原安装目录`  
2. 重载窗口  
3. （可选）把颜色主题改回 `Cursor Dark`，图标改回你原来的  

或把设置里的 `cursorSkin.enabled` 设为 `false`，再执行一次「应用皮肤」/重载。

---

## 八、重新安装扩展（需要时）

在 PowerShell 中：

```powershell
cd C:\Users\do996\Documents\cursor-skin
npm run build:theme
npm run package
cursor --install-extension .\cursor-skin-0.1.0.vsix --force
```

然后 `Developer: Reload Window`。

---

## 九、注意点

1. **背景图需要改安装目录**（写 `workbench.html`）。扩展会自动备份、还原，并更新校验和。  
2. **Cursor 自动更新后**，补丁可能被覆盖；扩展启动时会尝试重新打上。若背景没了，执行一次 `Cursor Skin: 应用皮肤` 再重载。  
3. **建议先在副本上试**：设置  
   `cursorSkin.installPath` = `C:\Users\do996\AppData\Local\Programs\cursor-skin`  
   再打开那份 Cursor，避免直接改主力安装。  
4. 本地图片会内联进 CSS；图太大时生成的样式文件会变大，建议壁纸控制在几 MB 内。

---

## 十、出问题对照

| 现象 | 处理 |
| --- | --- |
| 没有背景图 | 确认已设 `backgroundImage`，执行「应用皮肤」，然后**完全退出 Cursor 再打开**（不只是重载）。面板是实底时旧版会把图盖住，请用最新扩展。 |
| 颜色不对 | 确认主题是 **Skin Dark** |
| 图标没变 | 确认图标主题是 **Air File Icons** |
| 提示安装已损坏 | 确认 `fixChecksums` 为 `true`，执行「应用皮肤」后**完全退出再打开**。这是改了安装文件的正常提示，校验和同步后应消失。 |
| 写入失败 / 权限错误 | 关掉 Cursor 后重开，或用可写的副本安装路径 |
| 更新后皮肤消失 | 执行「应用皮肤」并完全重启 |

查看状态：`Cursor Skin: 查看当前状态`。

---

## 相关路径

| 内容 | 路径 |
| --- | --- |
| 扩展源码 | `C:\Users\do996\Documents\cursor-skin` |
| 默认壁纸 | `C:\Users\do996\Documents\cursor-skin\media\wallpaper-default.png` |
| 用户设置 | `%APPDATA%\Cursor\User\settings.json` |
| 已装扩展 | `%USERPROFILE%\.cursor\extensions\local.cursor-skin-0.1.0` |
| 当前主力安装 | `C:\Users\do996\AppData\Local\Programs\cursor` |
| 实验副本安装 | `C:\Users\do996\AppData\Local\Programs\cursor-skin` |
